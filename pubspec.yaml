name: progress_button
description: A progress button using streams
version: 1.0.0
author: "Lucas Rodrigues"

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  vibrate: ^0.0.4
  vector_math: ^2.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: